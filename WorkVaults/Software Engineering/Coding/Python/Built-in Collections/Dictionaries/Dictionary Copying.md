- There are two means of copying a dictionary, of most commonly we see the second. 

- The first is to use the `dict.copy()` method.
- The second is to simply pass an existing dictionary to the [[dict()]] constructor

